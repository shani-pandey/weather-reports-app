import { FilterSeacrhPipe } from './filter-seacrh.pipe';

describe('FilterSeacrhPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSeacrhPipe();
    expect(pipe).toBeTruthy();
  });
});
